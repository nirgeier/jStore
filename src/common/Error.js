var jStore = jStore || {};

!(function (ns, utils) {

    /**
     * @module Helper
     * @class Error
     */
    var Error;

    ns.Error = Error = function () {
    };

    Error.prototype = {
        constructor:Error
    };

}).apply(jStore, [jStore, jStore.utils]);
